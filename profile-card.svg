<svg width="960" height="360" viewBox="0 0 960 360" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="bgGrad" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#f9fafb"/>
      <stop offset="100%" stop-color="#eef2f7"/>
    </linearGradient>

    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="8" stdDeviation="16" flood-color="#000" flood-opacity="0.08"/>
    </filter>
  </defs>

  <!-- Card -->
  <rect x="20" y="20" width="920" height="320" rx="22"
        fill="url(#bgGrad)" stroke="#e5e7eb" filter="url(#shadow)"/>

  <!-- Fade-in -->
  <g opacity="0">
    <animate attributeName="opacity" from="0" to="1" dur="0.8s" fill="freeze"/>

    <!-- Title (emoji removed for GitHub compatibility) -->
    <text x="480" y="72" text-anchor="middle"
          font-size="26" font-weight="600"
          fill="#111827"
          font-family="system-ui, -apple-system, Segoe UI, Roboto, sans-serif">
      Hello World! Welcome to my GitHub
    </text>

    <!-- Divider -->
    <line x1="100" y1="96" x2="860" y2="96" stroke="#e5e7eb"/>

    <!-- Content -->
    <g font-size="16"
       fill="#1f2937"
       font-family="system-ui, -apple-system, Segoe UI, Roboto, sans-serif">

      <text x="140" y="145">💻 Full-Stack Developer | React • Next.js • Node.js</text>
      <text x="140" y="180">🌍 Op
